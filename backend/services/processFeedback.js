async function processFeedback(buffer) {
  // temporary mock

  return {
    transcript: "Food quality was excellent",

    sentimentLabel: "positive",

    sentimentScore: 0.91,
  };
}

module.exports = processFeedback;
